wget https://github.com/google/glog/archive/v0.3.4.tar.gz
tar zxvf v0.3.4.tar.gz
cd glog-0.3.4
./configure && make && make install

apt-get install -y build-essential autotools-dev libtool libconfig++-dev libboost-all-dev libcurl4-openssl-dev libssl-dev

run "poolwatcher -c <config file>" in the SAME directory as ckpool.  It will create a file called emptyblock.json that ckpool will read to get empty blocks.
